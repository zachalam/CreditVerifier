# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build credit-verifier-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t credit-verifier-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system credit-verifier-backend && \
          adduser --system -G credit-verifier-backend credit-verifier-backend

COPY dist/apps/credit-verifier-backend credit-verifier-backend
RUN chown -R credit-verifier-backend:credit-verifier-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix credit-verifier-backend --omit=dev -f install

CMD [ "node", "credit-verifier-backend" ]
